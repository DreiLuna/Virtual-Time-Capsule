server {
  listen 80;
  server_name _;
  root /usr/share/nginx/html;
  index index.html;

  # Try files, fallback to index.html for SPA routes
  location / {
    try_files $uri $uri/ /index.html;
  }

  # Optional: return index.html for 404s (covers some clients)
  error_page 404 /index.html;
}
